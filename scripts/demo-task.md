# Prompt to give the autonomous agent

Implement `requirements/001-saved-tool-runs.md` from requirement to deploy.

You own the full task. Use the available Google Stitch, Supabase, GitHub, and Vercel tools as needed. Inspect the repository first, plan the work, implement all required layers, run quality checks, fix failures without asking me to code, create a GitHub pull request, validate the Vercel preview deployment, and return acceptance-criteria evidence plus the preview URL.

Do not deploy to production if any required check is failing.
